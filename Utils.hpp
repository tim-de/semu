#ifndef _STBRROB_UTILS_H
#define _STBRROB_UTILS_H

#include<string>
#include<iostream>

namespace Width {
	enum Enum {
		b8 = 0,
		b16,
		b32
	};
}

namespace Element {
	enum Enum {
		min = 0,
		sub,
		addr
	};
}


#endif
